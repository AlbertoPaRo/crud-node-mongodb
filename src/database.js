const { connect } = require("mongoose");

const db = connect("mongodb://localhost/crud-mongo");

// (async () => {
//   //   try {

//   //   } catch (error) {
//   //     console.error(error);
//   //   }
// })();
